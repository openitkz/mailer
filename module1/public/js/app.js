$(document).ready(function(){

	/*var $contacts=$('.contacts');

	$contacts.find('button').on('click',function(e){
		e.preventDefault(), $input=$contacts.find('input');
		var contact=$input.val();
		$contacts.find('ul').append(getContactItem(contact));

		$contacts.find('span.remove').off('click').on('click',function(e){
			var $target=$(e.currentTarget);
			$target.closest('li').remove();
		});
	});

	function getContactItem(name){
		var res='<li class="list-group-item">';
		res+='<label class="checkbox-inline"><input type="checkbox" name="contacts[]" checked value="'+name+'">';
		res+=name+'</label><span class="remove"> x </span>';
		res+='</li>';
		return res;
	}*/


});